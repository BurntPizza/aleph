
use std::error::Error;

use lang::{self, Env, Form};

pub fn census(env: &mut Env, form: Vec<Form>) -> Result<Vec<Form>, Box<Error>> {
    // assert directives only in top-level
    // execute directives



    unimplemented!()
}
